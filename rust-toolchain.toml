# Pinned so a new stable's lints cannot turn CI red on a commit that changed nothing. Bump deliberately, in its own
# commit, and fix what the new clippy finds there. rustup installs this on the first cargo command in the repo.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
